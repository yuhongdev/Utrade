Ext.define('TCPlus.controller.WatchList', {
    extend: 'Ext.app.Controller',
    views: ['quote.WatchList'],
    models: ['WatchListRecord'],
    init: function() {
        this.control({
        });
    }
});